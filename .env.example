# Copiar a .env en el VPS y completar con el dominio real.
# Caddy usa este valor para pedir el certificado TLS, tiene que ser
# exactamente el dominio que apunta al servidor.
DOMAIN=tatianaorquera.com
